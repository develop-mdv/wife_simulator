# TG Wife AI

Telegram-бот для автоответов от имени пользовательского аккаунта (не бота). Отвечает в стиле любящей, заботливой жены. Использует **Gemini 2.5 Flash** для генерации ответов.

## Возможности

- 💬 Автоответы через Gemini API в стиле живой переписки
- 🎯 Отвечает только одному человеку (мужу)
- 🌙 Тихие часы с режимом игнорирования или очереди
- ⌨️ Имитация набора текста ("печатает...")
- 💾 Хранение контекста диалога в SQLite
- 🛡️ Rate limiting и защита от флуда
- 🤖 **Админ-бот** для управления настройками через Telegram

## Быстрый старт на Ubuntu VPS

### 1. Установка Docker

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com | sudo sh

# Добавить себя в группу docker (перелогиниться после)
sudo usermod -aG docker $USER
```

### 2. Клонирование проекта

```bash
git clone <your-repo-url> tg-wife-ai
cd tg-wife-ai
```

### 3. Настройка окружения

```bash
# Скопировать пример конфига
cp .env.example .env

# Отредактировать конфиг
nano .env
```

**Обязательные параметры:**
- `TG_API_ID` и `TG_API_HASH` — получить на [my.telegram.org/apps](https://my.telegram.org/apps)
- `GEMINI_API_KEY` — получить на [aistudio.google.com/apikey](https://aistudio.google.com/apikey)
- `TARGET_USER_ID` или `TARGET_USERNAME` — кому отвечать

### 4. Первый запуск (авторизация)

```bash
# Интерактивный запуск для ввода кода авторизации
docker compose run --rm tg-wife-ai
```

Телеграм отправит код на ваш аккаунт. Введите его в терминале.
Если включена 2FA — введите пароль.

После успешной авторизации нажмите `Ctrl+C`.

### 5. Запуск в фоне

```bash
# Запуск
docker compose up -d --build

# Просмотр логов
docker compose logs -f

# Остановка
docker compose down

# Перезапуск
docker compose restart
```

---

## Админ-бот 🤖

Админ-бот позволяет управлять автоответчиком через Telegram без редактирования `.env` и перезапуска контейнера.

### Настройка админ-бота

#### 1. Создание бота через BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя и username для бота
4. Скопируйте токен (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 2. Узнать свой Telegram ID

Используйте один из способов:
- Бот [@userinfobot](https://t.me/userinfobot) — перешлите ему любое сообщение
- После настройки админ-бота — команда `/whoami`

#### 3. Добавить в .env

```bash
ADMIN_BOT_TOKEN=ваш_токен_от_botfather
ADMIN_USER_IDS=ваш_telegram_id
```

Можно указать несколько админов через запятую: `ADMIN_USER_IDS=123,456,789`

### Команды админ-бота

| Команда | Описание |
|---------|----------|
| `/start` | Меню с кнопками и статус |
| `/status` | Текущий статус (AI on/off, пауза, тихие часы) |
| `/on` | Включить автоответы |
| `/off` | Выключить автоответы |
| `/pause 30m` | Пауза на 30 минут |
| `/pause 2h` | Пауза на 2 часа |
| `/pause until 23:00` | Пауза до 23:00 |
| `/resume` | Снять паузу |
| `/set <key> <value>` | Изменить настройку |
| `/whoami` | Ваш Telegram ID |
| `/last_sender` | ID последнего отправителя |
| `/help` | Справка по командам |

### Примеры /set

```
/set target_id 123456789
/set target_username husband
/set quiet_start 23:00
/set quiet_end 08:00
/set timezone Europe/Amsterdam
/set quiet_mode queue
/set context_turns 40
/set style_profile  (затем отправить текст)
```

### Inline-меню

После `/start` появится меню с кнопками:
- **AI: ON/OFF** — переключение автоответов
- **Пауза 30м / 2ч / 12ч** — быстрая пауза
- **Снять паузу** — отменить паузу
- **Тихие часы** — информация и настройки
- **Target User** — текущий целевой пользователь
- **Timezone** — часовой пояс
- **Обновить** — обновить статус

### Manual Override

Если вы сами отправляете сообщение мужу — бот автоматически встаёт на паузу на 15 минут. Это предотвращает "двойные ответы", когда вы общаетесь сами.

---

## Конфигурация

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TG_API_ID` | Telegram API ID | обязательно |
| `TG_API_HASH` | Telegram API Hash | обязательно |
| `GEMINI_API_KEY` | Gemini API ключ | обязательно |
| `TARGET_USER_ID` | ID пользователя-мужа | — |
| `TARGET_USERNAME` | Username без @ | — |
| `MODEL_NAME` | Модель Gemini | `gemini-2.5-flash` |
| `STYLE_PROFILE` | Дополнительные черты характера | — |
| `CONTEXT_TURNS` | Кол-во сообщений в контексте | `40` |
| `TIMEZONE` | Часовой пояс | `Europe/Moscow` |
| `QUIET_HOURS_START` | Начало тихих часов | — |
| `QUIET_HOURS_END` | Конец тихих часов | — |
| `QUIET_MODE` | `ignore` или `queue` | `queue` |
| `RATE_LIMIT_COUNT` | Макс. ответов | `4` |
| `RATE_LIMIT_WINDOW_SEC` | За период (сек) | `30` |
| `ADMIN_BOT_TOKEN` | Токен админ-бота | — |
| `ADMIN_USER_IDS` | ID админов через запятую | — |

---

## FAQ

### Где лежит telegram.session?

В папке `./data/telegram.session`. Этот файл содержит авторизацию — **не делитесь им**.

### Как сменить мужа?

Через админ-бота:
```
/set target_id новый_id
```

Или отредактируйте `.env` и перезапустите:
```bash
docker compose restart
```

### Как узнать user_id?

- Через админ-бота: `/whoami` (ваш ID) или `/last_sender` (ID последнего отправителя)
- Бот [@userinfobot](https://t.me/userinfobot) — перешлите ему сообщение от нужного человека

### Как настроить тихие часы?

Через админ-бота:
```
/set quiet_start 23:00
/set quiet_end 08:00
/set quiet_mode queue
```

Или в `.env`:
```bash
QUIET_HOURS_START=23:00
QUIET_HOURS_END=08:00
QUIET_MODE=queue  # или ignore
```

- `queue` — сообщения копятся, утром отправляется один общий ответ
- `ignore` — сообщения пропускаются

### Как добавить индивидуальность?

Через админ-бота:
```
/set style_profile
```
Затем отправьте текст профиля.

Или в `.env`:
```bash
STYLE_PROFILE=Люблю готовить. Часто спрашиваю что на ужин. Обожаю котиков.
```

### Контейнер падает — что делать?

Проверьте логи:
```bash
docker compose logs --tail=100
```

Частые причины:
- Неверные `TG_API_ID`/`TG_API_HASH`
- Невалидный `GEMINI_API_KEY`
- Telethon session протух — удалите `./data/telegram.session` и авторизуйтесь заново

### Что если ADMIN_BOT_TOKEN не задан?

Приложение запустится без админ-бота. Все настройки будут браться только из `.env`, управление только через редактирование файла и перезапуск.

### Как работает сохранение настроек?

- Настройки хранятся в SQLite (`./data/messages.db`, таблица `settings`)
- При старте: загружаются значения из `.env`, поверх накладываются значения из БД
- Изменения через админ-бота сохраняются в БД и действуют сразу
- Настройки переживают перезапуск контейнера
