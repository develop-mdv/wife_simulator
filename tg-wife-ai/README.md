# TG Wife AI (Multi-User Edition)

Telegram-бот, который позволяет **любому пользователю** создать своего персонального AI-ассистента ("Wife AI") для автоматических ответов выбранному контакту.

## ✨ Возможности

- **Персональный бот для каждого**: Просто напишите админ-боту, чтобы создать своего ассистента.
- **Простая настройка**: Пошаговый мастер настройки прямо в Telegram.
- **Безопасно**: Ваши данные (API keys, сессии) хранятся локально на сервере.
- **Умные ответы**: Использует Google Gemini 2.5 Flash для генерации ответов в вашем стиле.
- **Полный контроль**:
  - Включение/выключение в один клик
  - Тихие часы (ночной режим)
  - Пауза (30 мин, 2 часа...)
  - Ручной перехват (авто-пауза, если вы начали писать сами)

## 🚀 Установка (для администратора сервера)

Вам понадобится сервер с Docker и один бот от @BotFather.

### 1. Подготовка

1. Создайте бота в [@BotFather](https://t.me/BotFather) и получите токен.
2. Получите API Key для Gemini в [Google AI Studio](https://aistudio.google.com/apikey).

### 2. Запуск

```bash
# Клонировать репозиторий
git clone <url> tg-wife-ai
cd tg-wife-ai

# Создать .env
cp .env.example .env
nano .env
```

В `.env` нужно всего две строки:
```env
ADMIN_BOT_TOKEN=123456:ABC-DEF...
GEMINI_API_KEY=AIzaSy...
```

Запустите контейнер:
```bash
docker compose up -d --build
```

## 📱 Как пользоваться (для пользователей)

1. Откройте своего бота в Telegram.
2. Нажмите `/start`.
3. Следуйте инструкциям мастера настройки:
   - Введите `api_id` и `api_hash` (бот подскажет, где взять).
   - Войдите в свой аккаунт (введите номер и код).
   - Выберите контакт, которому бот будет отвечать (муж/жена/другой).
4. Готово! Управляйте ботом через меню.

### Меню управления
- **AI: ON/OFF** — Главный рубильник.
- **Пауза** — Временно отключить бота (удобно, если вы встретились лично).
- **Тихие часы** — Бот не будет отвечать ночью (можно настроить время).
- **Стиль общения** — Можно задать, как бот должен отвечать (например: "Отвечай дерзко", "Будь очень милой").

## 🛠 Технические детали

- **Стек**: Python 3.11, Telethon, python-telegram-bot, SQLite.
- **Архитектура**:
  - `AdminBot`: Интерфейс управления для всех пользователей.
  - `TelethonManager`: Управляет пулом клиентов (по одному на пользователя).
  - `Database`: Хранит сессии и настройки (messages.db).
- **Приватность**: Бот читает сообщения **только** от целевого контакта (target_user_id) и только в личных чатах.

## 📄 Лицензия
MIT
